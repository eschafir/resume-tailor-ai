# Copy this file to .env and fill in your key. .env is gitignored — never commit real keys.
# Get a DeepSeek API key at https://platform.deepseek.com

DEEPSEEK_API_KEY=

# Optional: override the default model used by the agents (default: deepseek-v4-pro)
# RESUME_TAILOR_MODEL=deepseek-v4-flash
